/// Process exit code indicating successful execution.
pub const SUCCESS: i32 = 0;

/// Process exit code indicating a runtime or compilation failure.
pub const FAILURE: i32 = 1;

/// Process exit code indicating invalid command-line usage (e.g., bad arguments).
pub const USAGE_ERROR: i32 = 2;
